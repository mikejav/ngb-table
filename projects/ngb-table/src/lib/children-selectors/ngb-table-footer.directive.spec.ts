import { NgbTableFooterDirective } from './ngb-table-footer.directive';

describe('NgbTableFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbTableFooterDirective();
    expect(directive).toBeTruthy();
  });
});
