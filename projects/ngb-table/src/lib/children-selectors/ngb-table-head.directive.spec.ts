import { NgbTableHeadDirective } from './ngb-table-head.directive';

describe('NgbTableHeadDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbTableHeadDirective();
    expect(directive).toBeTruthy();
  });
});
