import { NgbTableSelectActionsHeaderDirective } from './ngb-table-select-actions-header.directive';

describe('NgbTableSelectActionsHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbTableSelectActionsHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
