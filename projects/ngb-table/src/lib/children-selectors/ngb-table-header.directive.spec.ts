import { NgbTableHeaderDirective } from './ngb-table-header.directive';

describe('NgbTableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbTableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
