import { NgbTableSubheaderDirective } from './ngb-table-subheader.directive';

describe('NgbTableSubheaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbTableSubheaderDirective();
    expect(directive).toBeTruthy();
  });
});
