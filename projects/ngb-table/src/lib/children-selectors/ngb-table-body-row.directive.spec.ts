import { NgbTableBodyRowDirective } from './ngb-table-body-row.directive';

describe('NgbTableBodyRowDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbTableBodyRowDirective();
    expect(directive).toBeTruthy();
  });
});
